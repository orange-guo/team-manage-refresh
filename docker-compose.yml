services:
  app:
    build: .
    container_name: team-manage-app
    ports:
      - "61210:8008"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    restart: always
    environment:
      - DATABASE_URL=sqlite+aiosqlite:////app/data/team_manage.db
